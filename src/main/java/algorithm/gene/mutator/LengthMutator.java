package algorithm.gene.mutator;

import algorithm.gene.Mutator;
import algorithm.gene.feature.Length;
import algorithm.gene.feature.Note;

public class LengthMutator implements Mutator<Length> {
    @Override
    public Length mutate(Length feature) {
        return null;
    }
}
